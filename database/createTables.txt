use cs183;

create table users
(
	id int NOT NULL AUTO_INCREMENT,
	username VARCHAR(255) NOT NULL,
	password VARCHAR(255) NOT NULL,
	CONSTRAINT UID UNIQUE (id, username)
);

create table most_recent_network_status
(
	username VARCHAR(255) NOT NULL,
	id INT NOT NULL,
	status VARCHAR(255)
	


);

